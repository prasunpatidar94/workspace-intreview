server:
  port: 8080
spring:
  profiles:
    active: default
  application:
    name: "accounts"

  datasource:
    url: jdbc:mysql://localhost:3306/sunbank?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: dev
    password: dev
    driver-class-name: com.mysql.cj.jdbc.Driver

  config:
    import: "configserver:http://localhost:8071/"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQLDialect
  sql:
    init:
      mode: always            # Options: always, embedded, never
      schema-locations: classpath:schema.sql
      data-locations: classpath:data.sql
      continue-on-error: false


info:
  app:
    name: "accounts"
    description: "SUNBANK Account Application"
    version: "1.0.0 V"

#eureka configuration ------------------
eureka:
  instance:
    preferIpAddress: true
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8070/eureka
#euraka end --------------------

management:
  endpoints:
    web:
      exposure:
        include: "*"

#http://localhos:8080/actuator/shutdown  -post request
  endpoint:
    shutdown:
      enabled: true
  info:
    env:
      enabled: true

endpoint:
  shutdown:
    enabled: true

build:
  version: "1.0 V"



accounts:
  massage: "Welcome In SunBank Accounts Application..!"
  contactDetails:
    name: "Prasun Patidar"
    email: "codebysun@gmail.com"
  onCallSupport:
    - 88888-88888
    - 99999-99999
